package kr.ac.tukorea.ge.scgyong.samplegame

import android.view.View
import android.content.Context

class GameView(context: Context): View(context) {
    // View 를 상속하려면 생성자에서 Context 를 받아서 넘겨 줘야 한다.
}
